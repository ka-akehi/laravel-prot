version: "3.8"

services:
    laravel-app:
        build: .
        container_name: laravel-app
        volumes:
            - .:/var/www/html
        working_dir: /var/www/html
        ports:
            - "8000:8000"
